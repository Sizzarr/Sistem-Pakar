DATABASE_URL=mysql+pymysql://appuser:apppass@db:3306/sistem_pakar_tidur
JWT_SECRET=change-me-super-secret
JWT_EXPIRE_MINUTES=120

# Optional auto-bootstrap first admin (created only if users table is empty)
FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=admin12345

# MySQL container settings (used by docker-compose defaults)
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=sistem_pakar_tidur
MYSQL_USER=appuser
MYSQL_PASSWORD=apppass
